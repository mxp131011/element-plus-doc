import { cnDocument } from './zh-CN';
import { enDocument } from './en-US';
import { type LocalDocument } from '@/types/index';

/**
 * 所有文档
 */
export const localDocument: LocalDocument = {
  'zh-CN': cnDocument,
  'en-US': enDocument,
};
