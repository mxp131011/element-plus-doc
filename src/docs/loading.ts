import type { TagDoc } from '@/types/tag-doc';
const doc: TagDoc.TagDocInstance = {
  url: '',
  slots: [
    {
      name: '',
      description: {
        cn: "the DOM node Loading needs to cover. Accepts a DOM object or a string. If it's a string, it will be passed to <code>document.querySelector</code> to get the corresponding DOM node",
        en: "the DOM node Loading needs to cover. Accepts a DOM object or a string. If it's a string, it will be passed to <code>document.querySelector</code> to get the corresponding DOM node",
      },
    },
    {
      name: '',
      description: {
        cn: 'same as the <code>body</code> modifier of <code>v-loading</code>',
        en: 'same as the <code>body</code> modifier of <code>v-loading</code>',
      },
    },
    {
      name: '',
      description: {
        cn: 'same as the <code>fullscreen</code> modifier of <code>v-loading</code>',
        en: 'same as the <code>fullscreen</code> modifier of <code>v-loading</code>',
      },
    },
    {
      name: '',
      description: {
        cn: 'same as the <code>lock</code> modifier of <code>v-loading</code>',
        en: 'same as the <code>lock</code> modifier of <code>v-loading</code>',
      },
    },
    {
      name: '',
      description: { cn: 'loading text that displays under the spinner', en: 'loading text that displays under the spinner' },
    },
    {
      name: '',
      description: { cn: 'class name of the custom spinner', en: 'class name of the custom spinner' },
    },
    {
      name: '',
      description: { cn: 'background color of the mask', en: 'background color of the mask' },
    },
    {
      name: '',
      description: { cn: 'custom class name for Loading', en: 'custom class name for Loading' },
    },
  ],
};
export default doc;
